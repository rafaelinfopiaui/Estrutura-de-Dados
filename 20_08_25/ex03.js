// Remove o último elemento
let numeros = [1, 2, 4, 56];
console.log("Elemento removido: ", numeros.pop());

// Remoção específica
let lista = [1, 2, 3, 4, 5];
lista.splice(2, 2);
console.log("Lista alterada pelo splice: ", lista);
